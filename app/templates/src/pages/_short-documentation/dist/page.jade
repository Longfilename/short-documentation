doctype html
html(lang="en")
    head
        title
        script(src="//code.jquery.com/jquery-2.1.4.min.js")
    body
        ul
            - (content && content.pages) && content.pages.map(function (page) {
                - page.htmlArray.map(function (html) {
                    li
                        a(href="./html/" + html)=page.title
                        - if (page.jade.length > 1) {
                            |  (
                            =html
                            |  )
                        - }
                - })
            - })
